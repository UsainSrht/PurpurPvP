<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.usainsrht</groupId>
    <artifactId>PurpurPvP</artifactId>
    <version>1.0</version>
    <packaging>jar</packaging>

    <name>PurpurPvP</name>

    <properties>
        <java.version>21</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <build>
        <defaultGoal>clean package</defaultGoal>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.3</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <relocations>
                        <relocation>
                            <pattern>dev.triumphteam.gui</pattern>
                            <shadedPattern>com.usainsrht.purpurpvp.libs.gui</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>me.usainsrht.yamlmessage</pattern>
                            <shadedPattern>com.usainsrht.purpurpvp.libs.yamlmessage</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>me.usainsrht.itemapi</pattern>
                            <shadedPattern>com.usainsrht.purpurpvp.libs.itemapi</shadedPattern>
                        </relocation>
                    </relocations>
                    <artifactSet>
                        <excludes>
                            <exclude>com.google.guava:*</exclude>
                            <exclude>com.google.errorprone:*</exclude>
                            <exclude>com.google.j2objc:*</exclude>
                            <exclude>org.jetbrains:*</exclude>
                            <exclude>org.jspecify:*</exclude>
                        </excludes>
                    </artifactSet>
                    <filters>
                        <filter>
                            <artifact>*:*</artifact>
                            <excludes>
                                <exclude>META-INF/MANIFEST.MF</exclude>
                            </excludes>
                        </filter>
                    </filters>
                </configuration>
            </plugin>
        </plugins>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
    </build>

    <repositories>
        <repository>
            <id>papermc-repo</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <repository>
            <id>triumphteam-repo</id>
            <url>https://repo.triumphteam.dev/snapshots/</url>
        </repository>
        <repository>
            <id>codemc-public</id>
            <url>https://repo.codemc.io/repository/maven-public/</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>26.2.build.123-stable</version>
            <scope>provided</scope>
        </dependency>

        <!-- Database: HikariCP Connection Pool (Loaded at runtime via Paper PluginLoader) -->
        <dependency>
            <groupId>com.zaxxer</groupId>
            <artifactId>HikariCP</artifactId>
            <version>5.1.0</version>
            <scope>provided</scope>
        </dependency>

        <!-- Database: H2 (local/dev) (Loaded at runtime via Paper PluginLoader) -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <version>2.2.224</version>
            <scope>provided</scope>
        </dependency>

        <!-- Database: MySQL Connector (Loaded at runtime via Paper PluginLoader) -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>8.3.0</version>
            <scope>provided</scope>
        </dependency>

        <!-- GUI Framework: Triumph-GUI (Shaded & Relocated) -->
        <dependency>
            <groupId>dev.triumphteam</groupId>
            <artifactId>triumph-gui-paper</artifactId>
            <version>4.0.0-SNAPSHOT</version>
        </dependency>

        <!-- YamlMessageAPI (Shaded & Relocated) -->
        <dependency>
            <groupId>me.usainsrht</groupId>
            <artifactId>YamlMessageAPI</artifactId>
            <version>1.0.0</version>
        </dependency>

        <!-- ItemAPI: itemtext (Shaded & Relocated) -->
        <dependency>
            <groupId>me.usainsrht.itemapi</groupId>
            <artifactId>itemtext</artifactId>
            <version>1.0.6</version>
        </dependency>

        <!-- ItemAPI: yamlitem (Shaded & Relocated) -->
        <dependency>
            <groupId>me.usainsrht.itemapi</groupId>
            <artifactId>yamlitem</artifactId>
            <version>1.0.6</version>
        </dependency>

        <!-- JUnit 5 for Testing -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.11.4</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
